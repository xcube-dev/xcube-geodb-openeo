geodb_openeo:
  vectorcube_provider_class: tests.core.mock_vc_provider.MockProvider
  SERVER_URL: http://xcube-geoDB-openEO.de
  SERVER_ID: xcube-geodb-openeo
  SERVER_TITLE: xcube geoDB Server, openEO API
  SERVER_DESCRIPTION: Catalog of geoDB collections.
  kc_client_id:     placeholder
  kc_base_url:      placeholder
  kc_internal_client_id:     placeholder

api_spec:
  includes:
    - geodb-openeo
    - meta
    - auth

tornado:
  xheaders: true